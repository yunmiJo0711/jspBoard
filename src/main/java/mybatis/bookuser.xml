<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="bookuser">
 	
 	<select id="login" parameterType="java.util.Map" resultType="org.iclass.dto.BookUser">
 		select * from bookuser
		where id=#{id} and password = #{password}
 	</select>
 	
	<insert id="insert" parameterType="org.iclass.dto.BookUser">
		insert into bookuser(id, name, email, age, password) 
        values (#{id}, #{name}, #{email}, #{age}, #{password})
	</insert>
 	
 </mapper>

 
 
 
 
 